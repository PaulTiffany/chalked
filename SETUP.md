# Chalked — Setup

## How to Run
Open index.html in any browser. No server needed.

To reset the board, open a private window OR clear localStorage (key: chalked_marks).

Type in the cyan field to add your mark; use the agent buttons to add and tidy.

That's it — run locally, no server exposure.
